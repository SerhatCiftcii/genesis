@model List<Course>
    
    @foreach(var kurs in Model) 
    {
        if(kurs.isActive){

        
   <!-- <div class="card mb-3" id="kurs @kurs.Id">
        <div class="row">
        <div class="col-md-4">
            <img src="~/img/@kurs.Image" class="img-fluid rounded-start" alt="">
        </div>
        <div class="col-md-8">
            <div class="car-body">
           <h5 class="card-title">@kurs.Title</h5>
          <div class="card-text">@kurs.Description</div>
        </div>
    </div>
    </div>
</div>   _Course parital viewına aldık buraı  -->
   @await Html.PartialAsync("_Course",kurs) 
}}